---
title: "Retrieve a bill payment"
description: "Check the current status of a bill payment."
---

**GET** `/v1/bills/{id}`

<Info>
  For real-time updates, prefer [webhooks](/webhooks) instead of polling.
</Info>

<RequestExample>
```bash cURL
curl -X GET https://api.tupay.africa/v1/bills/ABCDEFG250101 \
  -H 'Authorization: Bearer {token}'
```
</RequestExample>

### Path parameter

| Parameter | Description |
|-----------|-------------|
| `id` | Transaction ID or your internal reference |

---

<ResponseExample>
```json Response
{
  "id":        "ABCDEFG250101",
  "reference": "ref1234",
  "status":    20,
  "message":   "Success"
}
```
</ResponseExample>

<ResponseField name="id" type="string">
  Tupay transaction ID.
</ResponseField>

<ResponseField name="reference" type="string">
  Your internal reference.
</ResponseField>

<ResponseField name="status" type="number">
  See [Status Codes](/status).
</ResponseField>

<ResponseField name="message" type="string">
  Human-readable status.
</ResponseField>
